it('should handle empty input gracefully', function() {
  const updatedContent = replacePlaceholder('', {});
  assert.ok(updatedContent === '<w:document/>' || updatedContent === '<w:document></w:document>');
});
